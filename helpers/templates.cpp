/** TEMPLATES
*
* create compatible functions with any types
* create compatible classes with any types
*
* specialise your code :
*   insible
*   run during compilation
*   no code repeat
*
**/

/**
*
*   could be used has a return type function
*
*        template <typename T>
*        const float mean (const T& a, const T& b)
*        {
*            return ((a+b)/2.0);
*        }
*
*   call : std::cout << getmin<int> (10, -42) << std ::endl;
*
**/
